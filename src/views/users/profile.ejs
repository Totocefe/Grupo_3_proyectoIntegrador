<!DOCTYPE html>
<html lang="en">
    <%-include('../partials/head')%>
<body>
    <%-include('../partials/header')%>
    <main class="body-main">
        <section class="information">
            <div class="main-information">
                <div>
                    <% if(locals.user) { %> 
                    <img src="/images/users/<%= user.image %>" alt="">
                    <p>Hola <%= user.name %> !!</p>
                    <% } %>
                    <a href="/">
                        <button type="submit" style="background-color: rgb(99, 153, 99); color: white;" ;>Ir a la Pagina Pincipal</button>
                    </a>
                     <a href="/user/editUser/<%= user.id %>">
                         <button type="submit" style="background-color: rgb(99, 153, 99); color: white; ">Editar perfil</button>
                     </a>
                     <form action="" method="post">
                         <button type="submit" style="background-color: rgb(99, 153, 99); color: white; ">Eliminar usuario</button>
                     </form>
                </div>
            </div>
        </section>
    </main>
    <%-include('../partials/footer')%>
</body>
</html>